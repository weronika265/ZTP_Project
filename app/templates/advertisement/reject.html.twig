{% include 'default/_form.html.twig' with {
    page_title: 'title.advertisement_reject'|trans({'%id%': advertisement.id|default('')}),
    form: form,
    submit_label: 'action.reject'|trans,
    back_to_list_path: 'user_index',
} %}